import { ProjectActivityManagerRole } from './types'

export const projectActivityManagerRoleNames: Record<ProjectActivityManagerRole, string> = {
    [ProjectActivityManagerRole.Manager]: 'руководитель проекта',
}
